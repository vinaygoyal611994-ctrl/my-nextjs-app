[phases.build]
cmds = ["npm run build"]

[phases.install]
cmds = ["npm install", "npx prisma generate"]

[start]
cmd = "npx prisma migrate resolve --applied 0001_baseline 2>/dev/null; npx prisma migrate deploy && npm start"
